default_config:
    checkstyle:
        naming:
            isser_method_name: "^(?:is|has|should|may|supports|matches)"

path_configs:
    tests:
        paths: [Tests/*]
        checkstyle: false
        doc_comment_fixes: false
        verify_php_doc_comments: false
